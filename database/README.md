# Database

This project currently uses mock travel data in `backend/src/database.py` and `backend/src/database_additions.py`.

Use this folder for future persistent database work:

- `migrations/`: versioned schema changes.
- `seeds/`: repeatable sample data.
- `schema.sql`: optional canonical SQL schema when a real database is introduced.
