# Local dev overrides — thêm vào đây nếu cần điều chỉnh riêng cho máy dev.
# Port 8001 cho API đã được định nghĩa trong docker-compose.yml (không cần lặp lại ở đây).
# File này sẽ tự động được docker compose đọc cùng với docker-compose.yml.
